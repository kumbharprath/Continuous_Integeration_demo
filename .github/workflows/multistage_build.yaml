name: Multi Stage Demo

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:

      - name: Build Step
        run: echo "Application Build Successul"

  test:
    runs-on: ubuntu-latest
    needs: build

    steps:

        run: exit 1
        run: echo "Application Test Successul"

  deploy:
    runs-on: ubuntu-latest
    needs: test

    steps:
    
      - name: Deploy Step
        run: echo "Application Deploy Successul"